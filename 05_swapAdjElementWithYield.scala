/**
Repeat the preceding assignment, but produce a new array with the swapped
values. Use for / yield .
*/
var array = Array[Int](1,2,3,4,5)
var newArray = Array[Int](array.size)
def swapWithYield(array : Array[Int])={
	var newArray = {
		for()
	} 
}

swapWithYield(array)